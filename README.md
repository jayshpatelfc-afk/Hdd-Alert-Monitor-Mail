# Hdd-Alert-Monitor-Mail
This project is a Python-based utility designed to monitor the connection of an external hard drive and automatically notify the user via email when the drive is plugged into a computer. The script continuously runs in the background, checking for the presence of a specific drive letter assigned to the external HDD.
